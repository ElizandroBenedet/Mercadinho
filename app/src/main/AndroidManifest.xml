<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meiinc.mercadinho">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_mercadinho_round2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_mercadinho_round2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CriarContaActivity" />
        <activity android:name=".TelaPrincipal" />
        <activity android:name=".CriarListaActivity" />
        <activity android:name=".TelaGraficoActivity" />
        <activity android:name=".telaListaActivity" />
        <activity android:name=".AdicionarProdutoListaActivity"></activity>
    </application>

</manifest>